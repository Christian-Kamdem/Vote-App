document.addEventListener("DOMContentLoaded",()=>{
    const nom = getElementById.value("nom");
    const prenom = getElementById.value("prenom");
    const email = getElementById.value("email");
    const datenais = getElementById.value("datenais");
    const mdp = getElementById.value("mdp");
    const tel = getElementById.value("tel");
    const envoyer = getElementById.value("envoyer");
});